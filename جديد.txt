local remote = game:GetService("ReplicatedStorage"):WaitForChild("RE"):WaitForChild("1RPNam1eTex1t")
local selectedPlayer = nil 

local mensagens = {
	"",
	"",
	"N",
	"NE",
	"NEY",
	"NEYM",
	"NEYMA",
	"NEYMAR",
	"NEYMAR H",
	"NEYMAR HUB ",
	"NEYMAR HUB B",
	"NEYMAR HUB BR",
	"NEYMAR HUB BRO",
	"NEYMAR HUB BROO",
	"NEYMAR HUB BROOK",
	"NEYMAR HUB BROOKH",
	"NEYMAR HUB BROOKHA",
	"NEYMAR HUB BROOKHAV",
	"NEYMAR HUB BROOKHAVE",
	"NEYMAR HUB BROOKHAVEN",
	"NEYMAR HUB BROOKHAVEN",
	"NEYMAR HUB BROOKHAVEN",
}


for _, mensagem in ipairs(mensagens) do
	remote:FireServer("RolePlayBio", mensagem)
	wait(0.1)
end

local Libary = loadstring(game:HttpGet("https://raw.githubusercontent.com/hassounijamel-cpu/077/refs/heads/main/NeymarGUI.txt"))()
workspace.FallenPartsDestroyHeight = -math.huge

local Window = Libary:MakeWindow({
    Title = "NEYMAR HUB",
    SubTitle = "by: Kdkjdnde",
    LoadText = "Best Arabic script",
    Flags = "neymar_Broookhaven"
})
Window:AddMinimizeButton({
    Button = { Image = "rbxassetid://99353410676626", BackgroundTransparency = 0 },
    Corner = { CornerRadius = UDim.new(35, 1) },
})
local TabAll = Window:MakeTab({
    Name = "الحقوق + المضادات",
    Icon = "rbxassetid://10734952036",
    PremiumOnly = false
})

-- ================= الحقوق =================
TabAll:AddSection({ Name = "📌 الحقوق" })

TabAll:AddParagraph({
    Title = "المطور",
    Content = "نيمار | حسنين"
})

TabAll:AddButton({
    Name = "📋 نسخ تيك توك",
    Callback = function()
        setclipboard("WLV27")
    end
})

TabAll:AddButton({
    Name = "📋 نسخ تليجرام",
    Callback = function()
        setclipboard("@WLV66")
    end
})

-- ================= المضادات =================
TabAll:AddSection({ Name = "🛡️ المضادات" })

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local TweenService = game:GetService("TweenService")
local RS = game:GetService("ReplicatedStorage")
local Workspace = game:GetService("Workspace")
local LocalPlayer = Players.LocalPlayer

-- ===== مضاد بانج =====
local character, humanoid, root, invisible = nil, nil, nil, false
local savedParts = {}

local function cacheParts()
    savedParts = {}
    for _,v in ipairs(character:GetDescendants()) do
        if v:IsA("BasePart") and v.Transparency < 1 then
            table.insert(savedParts, v)
        end
    end
end

local function setInvisible(state)
    invisible = state
    for _,p in ipairs(savedParts) do
        p.Transparency = invisible and 0.5 or 0
    end
end

local function onCharacter(char)
    character = char
    humanoid = char:WaitForChild("Humanoid")
    root = char:WaitForChild("HumanoidRootPart")
    cacheParts()
    setInvisible(false)
end

LocalPlayer.CharacterAdded:Connect(onCharacter)
if LocalPlayer.Character then
    onCharacter(LocalPlayer.Character)
end

RunService.Heartbeat:Connect(function()
    if invisible and root and humanoid then
        local oldCF = root.CFrame
        local oldOffset = humanoid.CameraOffset
        local newCF = oldCF * CFrame.new(0, -200000, 0)
        local rel = newCF:ToObjectSpace(CFrame.new(oldCF.Position)).Position
        root.CFrame = newCF
        humanoid.CameraOffset = rel
        RunService.RenderStepped:Wait()
        root.CFrame = oldCF
        humanoid.CameraOffset = oldOffset
    end
end)

TabAll:AddToggle({
    Name = "مضاد بانج",
    Default = false,
    Callback = setInvisible
})

-- ===== مضاد نسخ سكنات =====
local Remotes = RS:WaitForChild("Remotes")
local resetRemote = Remotes:WaitForChild("ResetCharacterAppearance")
local enabledSkin = false

local function getAccSet(desc)
    local t={}
    for _,a in ipairs(desc:GetAccessories(true)) do
        if a.AssetId then t[a.AssetId]=true end
    end
    return t
end

local function countMatch(a,b)
    local c=0
    for id in pairs(a) do
        if b[id] then c+=1 end
    end
    return c
end

local function tweenTo(plr)
    if not (plr.Character and LocalPlayer.Character) then return end
    local hrp=LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
    local thrp=plr.Character:FindFirstChild("HumanoidRootPart")
    if not (hrp and thrp) then return end
    TweenService:Create(hrp,TweenInfo.new(0.2,Enum.EasingStyle.Linear),{CFrame=thrp.CFrame}):Play()
end

local function watch(plr)
    if plr==LocalPlayer then return end
    task.spawn(function()
        while enabledSkin and plr.Parent do
            if plr.Character and LocalPlayer.Character then
                local lhum=LocalPlayer.Character:FindFirstChildOfClass("Humanoid")
                local thum=plr.Character:FindFirstChildOfClass("Humanoid")
                if lhum and thum then
                    local lset=getAccSet(lhum:GetAppliedDescription())
                    local tset=getAccSet(thum:GetAppliedDescription())
                    if countMatch(lset,tset)>=3 then
                        resetRemote:FireServer()
                        task.wait(0.1)
                        tweenTo(plr)
                        task.wait(0.00001)
                    end
                end
            end
            task.wait(0.2)
        end
    end)
end

TabAll:AddToggle({
    Name="مضاد نسخ سكنات",
    Default=false,
    Callback=function(v)
        enabledSkin=v
        if v then
            for _,p in ipairs(Players:GetPlayers()) do watch(p) end
            Players.PlayerAdded:Connect(watch)
        end
    end
})

-- ===== مضاد ازعاج الصوت =====
local conexaoAutoRemocao
local alvo = "1Gu1nSound1s"
local objetosRestaurados = {} 

local function removerAlvo()
    for _, obj in ipairs(game:GetDescendants()) do
        if obj.Name == alvo then
            pcall(function()
                local clone = obj:Clone()
                local parentOriginal = obj.Parent
                table.insert(objetosRestaurados, {Clone = clone, Parent = parentOriginal})
                obj:Destroy()
            end)
        end
    end
end

local function restaurarAlvo()
    for _, dados in ipairs(objetosRestaurados) do
        pcall(function()
            dados.Clone.Parent = dados.Parent
        end)
    end
    objetosRestaurados = {}
end

TabAll:AddToggle({
    Name = "مضاد ازعاج الصوت",
    Default = false,
    Callback = function(v)
        if v then
            removerAlvo()
            conexaoAutoRemocao = game.DescendantAdded:Connect(function(obj)
                if obj.Name == alvo then
                    task.wait(0.1)
                    pcall(function()
                        local clone = obj:Clone()
                        local parentOriginal = obj.Parent
                        table.insert(objetosRestaurados, {Clone = clone, Parent = parentOriginal})
                        obj:Destroy()
                    end)
                end
            end)
        else
            if conexaoAutoRemocao then
                conexaoAutoRemocao:Disconnect()
                conexaoAutoRemocao = nil
            end
            restaurarAlvo()
        end
    end
})

-- ===== مضاد فويد =====
local lastSafePos = Vector3.new(-26.09, 2.79, 6.11)
local avoidConn

TabAll:AddToggle({
    Name = "مضاد فويد",
    Default = false,
    Callback = function(Value)
        if avoidConn then
            avoidConn:Disconnect()
            avoidConn = nil
        end
        if Value then
            avoidConn = RunService.Stepped:Connect(function()
                local char = LocalPlayer.Character
                local hrp = char and char:FindFirstChild("HumanoidRootPart")
                if not hrp then return end

                local pos = hrp.Position
                if pos.Y > -20 and pos.Y < 1000 and math.abs(pos.X) < 2000 and math.abs(pos.Z) < 2000 then
                    lastSafePos = pos
                end

                if pos.Y < -50 or pos.Y > 1500 or math.abs(pos.X) > 3000 or math.abs(pos.Z) > 3000 then
                    hrp.CFrame = CFrame.new(lastSafePos)
                    pcall(function()
                        game.StarterGui:SetCore("SendNotification", {
                            Title = "LOC4T HUB",
                            Text = "انت محمي من الموت تحت الارض",
                            Duration = 2
                        })
                    end)
                end
            end)
        end
    end
})

-- ===== مضاد فلنق + المضادات الأخرى =====
local backupTables = {Vehicles = {}, Canoes = {}, Jets = {}, Helis = {}, Balls = {}}
local TeleportCarro = {}
function TeleportCarro:MostrarNotificacao(msg)
    print("LOC4T "..msg)
end

local function AntiFlingLoop(name, getFolderFunc)
    local active = false
    task.spawn(function()
        while true do
            if active and LocalPlayer.Character then
                local folder = getFolderFunc()
                if folder then
                    for _, item in ipairs(folder:GetChildren()) do
                        local isMine = false
                        if name == "Vehicles" then
                            for _, seat in ipairs(item:GetDescendants()) do
                                if (seat:IsA("VehicleSeat") or seat:IsA("Seat")) and seat.Occupant and seat.Occupant.Parent == LocalPlayer.Character then
                                    isMine = true
                                    break
                                end
                            end
                        elseif name == "Canoes" then
                            local owner = item:FindFirstChild("Owner")
                            isMine = owner and owner.Value == LocalPlayer
                        elseif name == "Jets" or name == "Helis" then
                            isMine = item.Name == LocalPlayer.Name
                        end
                        if not isMine then
                            table.insert(backupTables[name], item:Clone())
                            item:Destroy()
                        end
                    end
                end
            end
            task.wait(0.03)
        end
    end)
    return function(state)
        active = state
        TeleportCarro:MostrarNotificacao(name.." "..(state and "ativado!" or "desativado!"))
        if not state then
            for _, item in ipairs(backupTables[name]) do
                local parentFolder = getFolderFunc()
                if parentFolder then item.Parent = parentFolder end
            end
            backupTables[name] = {}
        end
    end
end

TabAll:AddToggle({
    Name = "مضاد فلنق",
    Default = false,
    Callback = AntiFlingLoop("Vehicles", function()
        return Workspace:FindFirstChild("Vehicles")
    end)
})

TabAll:AddToggle({
    Name = "مضاد فلنق قارب الصيد",
    Default = false,
    Callback = AntiFlingLoop("Canoes", function()
        local workspaceCom = Workspace:FindFirstChild("WorkspaceCom")
        return workspaceCom and workspaceCom:FindFirstChild("001_CanoeStorage")
    end)
})

TabAll:AddToggle({
    Name = "مضاد فلنق طائرة",
    Default = false,
    Callback = AntiFlingLoop("Jets", function()
        local folder = Workspace:FindFirstChild("WorkspaceCom")
        if folder and folder:FindFirstChild("001_Airport") then
            local storage = folder["001_Airport"]:FindFirstChild("AirportHanger")
            if storage then return storage:FindFirstChild("001_JetStorage") and storage["001_JetStorage"]:FindFirstChild("JetAirport") end
        end
    end)
})

TabAll:AddToggle({
    Name = "مضاد فلنق هيليكوبتر",
    Default = false,
    Callback = AntiFlingLoop("Helis", function()
        local folder = Workspace:FindFirstChild("WorkspaceCom")
        return folder and folder:FindFirstChild("001_HeliStorage") and folder["001_HeliStorage"]:FindFirstChild("PoliceStationHeli")
    end)
})

TabAll:AddToggle({
    Name = "مضاد فلنق كرة",
    Default = false,
    Callback = AntiFlingLoop("Balls", function()
        local folder = Workspace:FindFirstChild("WorkspaceCom")
        return folder and folder:FindFirstChild("001_SoccerBalls")
    end)
})

-- ===== مضاد جلوس =====
local antiSitActive = false
TabAll:AddToggle({
    Name = "مضاد جلوس",
    Default = false,
    Callback = function(state)
        antiSitActive = state
        TeleportCarro:MostrarNotificacao("Anti Sit "..(state and "ativado!" or "desativado!"))
        task.spawn(function()
            while antiSitActive and LocalPlayer.Character do
                local humanoid = LocalPlayer.Character:FindFirstChildOfClass("Humanoid")
                if humanoid then
                    humanoid:SetStateEnabled(Enum.HumanoidStateType.Seated, false)
                    if humanoid:GetState() == Enum.HumanoidStateType.Seated then
                        humanoid:ChangeState(Enum.HumanoidStateType.GettingUp)
                    end
                end
                task.wait(0.05)
            end
            if not antiSitActive then
                local humanoid = LocalPlayer.Character and LocalPlayer.Character:FindFirstChildOfClass("Humanoid")
                if humanoid then
                    humanoid:SetStateEnabled(Enum.HumanoidStateType.Seated, true)
                end
            end
        end)
    end
})

-- ===== مضاد لاق الأدوات =====
TabAll:AddToggle({
    Name = "مضاد لاق الادوات",
    Default = false,
    Callback = function(state)
        local dedupLock = {}
        local IGNORED_PLAYER
        if not state then return end

        local function marcarIgnorado(player) IGNORED_PLAYER = player end
        local function isTargetTool(inst) return inst:IsA("Tool") end
        local function gatherTools(player)
            local found = {}
            local containers = {}
            if player.Character then table.insert(containers, player.Character) end
            local backpack = player:FindFirstChildOfClass("Backpack")
            if backpack then table.insert(containers, backpack) end
            local sg = player:FindFirstChild("StarterGear")
            if sg then table.insert(containers, sg) end
            for _, container in ipairs(containers) do
                for _, child in ipairs(container:GetChildren()) do
                    if isTargetTool(child) then table.insert(found, child) end
                end
            end
            return found
        end

        local function dedupePlayer(player)
            if player == IGNORED_PLAYER then return end
            if dedupLock[player] then return end
            dedupLock[player] = true
            local tools = gatherTools(player)
            if #tools > 1 then
                for i = 2, #tools do pcall(function() tools[i]:Destroy() end) end
            end
            dedupLock[player] = false
        end

        local function hookPlayer(player)
            if not IGNORED_PLAYER then marcarIgnorado(player) end
            task.defer(dedupePlayer, player)
            local function setupChar(char)
                task.delay(0.5, function() dedupePlayer(player) end)
                char.ChildAdded:Connect(function(child)
                    if isTargetTool(child) then task.delay(0.1, function() dedupePlayer(player) end) end
                end)
            end
            if player.Character then setupChar(player.Character) end
            player.CharacterAdded:Connect(setupChar)
            local backpack = player:WaitForChild("Backpack", 10)
            if backpack then
                backpack.ChildAdded:Connect(function(child)
                    if isTargetTool(child) then task.delay(0.1, function() dedupePlayer(player) end) end
                end)
            end
            local sg = player:FindFirstChild("StarterGear") or player:WaitForChild("StarterGear", 10)
            if sg then
                sg.ChildAdded:Connect(function(child)
                    if isTargetTool(child) then task.delay(0.1, function() dedupePlayer(player) end) end
                end)
            end
        end

        Players.PlayerAdded:Connect(hookPlayer)
        for _, plr in ipairs(Players:GetPlayers()) do hookPlayer(plr) end
        task.spawn(function()
            while state do
                for _, plr in ipairs(Players:GetPlayers()) do dedupePlayer(plr) end
                task.wait(2)
            end
        end)
    end
})

-- ===== مضاد الأبواب =====
TabAll:AddToggle({
    Name = "مضاد الابواب",
    Default = false,
    Callback = function(state)
        if not _G.hiddenDoors then _G.hiddenDoors = {} end
        if state then
            _G.hiddenDoors = {}
            for _, obj in ipairs(Workspace:GetDescendants()) do
                if obj:IsA("BasePart") and obj.Name:lower():find("door") then
                    local doorData = {
                        door = obj,
                        originalTransparency = obj.Transparency,
                        originalCanCollide = obj.CanCollide,
                        originalCastShadow = obj.CastShadow
                    }
                    obj.Transparency = 1
                    obj.CanCollide = false
                    obj.CastShadow = false
                    for _, child in ipairs(obj:GetChildren()) do
                        if child:IsA("BasePart") then
                            child.Transparency = 1
                            child.CanCollide = false
                        elseif child:IsA("SurfaceGui") or child:IsA("BillboardGui") then
                            child.Enabled = false
                        end
                    end
                    table.insert(_G.hiddenDoors, doorData)
                end
            end
        else
            for _, doorData in ipairs(_G.hiddenDoors or {}) do
                if doorData.door and doorData.door.Parent then
                    doorData.door.Transparency = doorData.originalTransparency
                    doorData.door.CanCollide = doorData.originalCanCollide
                    doorData.door.CastShadow = doorData.originalCastShadow
                    for _, child in ipairs(doorData.door:GetChildren()) do
                        if child:IsA("BasePart") then
                            child.Transparency = 0
                            child.CanCollide = true
                        elseif child:IsA("SurfaceGui") or child:IsA("BillboardGui") then
                            child.Enabled = true
                        end
                    end
                end
            end
            _G.hiddenDoors = {}
        end
    end
})

-- ===== مضاد الأطفال =====
local enabledKids = false
TabAll:AddToggle({
    Name="مضاد الأطفال",
    Default=false,
    Callback=function(v)
        enabledKids=v
    end
})

local MyTab = Window:MakeTab({ Title = "التلوينات", Icon = "rbxassetid://10734910187" })

--================ الخدمات =================--
local RS = game:GetService("ReplicatedStorage")
local RSvc = game:GetService("RunService")
local Plrs = game:GetService("Players")
local LP = Plrs.LocalPlayer

--================ التلوين (الاسم + البايو) =================--
MyTab:AddSection({ Name = "نظام تلوين متطور", Icon = "rbxassetid://10709782758" })

local ColorRemote = RS:WaitForChild("RE"):WaitForChild("1RPNam1eColo1r")

local timeTick = 0
local colorSpeed = 2 
local enableNameColor = false
local enableBioColor = false
local fastName = false
local fastBio = false

RSvc.RenderStepped:Connect(function(dt)
	timeTick += dt
	local spd = fastName and 20 or colorSpeed

	local r = math.sin(timeTick * spd) * 0.5 + 0.5
	local g = math.sin(timeTick * spd + 2) * 0.5 + 0.5
	local b = math.sin(timeTick * spd + 4) * 0.5 + 0.5

	local clr = Color3.new(r, g, b)

	if enableNameColor or fastName then
		ColorRemote:FireServer("PickingRPNameColor", clr)
	end
	if enableBioColor or fastBio then
		ColorRemote:FireServer("PickingRPBioColor", clr)
	end
end)

MyTab:AddSlider({
	Name = "سرعة اللون",
	Min = 0.1,
	Max = 500,
	Default = 2,
	Increment = 0.1,
	Callback = function(v)
		colorSpeed = v
	end
})

MyTab:AddToggle({
	Name = "تشغيل لون الاسم",
	Callback = function(v)
		enableNameColor = v
	end
})

MyTab:AddToggle({
	Name = "تشغيل لون البايو",
	Callback = function(v)
		enableBioColor = v
	end
})

--================ الكتابة المتحركة =================--
MyTab:AddSection({ Name = "نظام الكتابة المتحركة", Icon = "rbxassetid://10723345088" })

local TextRemote = RS:WaitForChild("RE"):WaitForChild("1RPNam1eTex1t")

local myBio, myName = "", ""

MyTab:AddTextBox({
	Name = "اكتب البايو",
	Callback = function(v)
		myBio = v
	end
})

MyTab:AddTextBox({
	Name = "اكتب الاسم",
	Callback = function(v)
		myName = v
	end
})

local function TextEffect(typeName, getText, reverse, state)
	task.spawn(function()
		while state() do
			local txt = getText()
			if txt ~= "" then
				for i = 1, #txt do
					if not state() then return end
					TextRemote:FireServer(typeName, string.sub(txt, 1, i))
					task.wait(0.08)
				end
				if reverse then
					for i = #txt, 1, -1 do
						if not state() then return end
						TextRemote:FireServer(typeName, string.sub(txt, 1, i))
						task.wait(0.08)
					end
				end
			else
				task.wait(0.2)
			end
		end
	end)
end

local bioLoop, bioReverse, nameLoop, nameReverse = false,false,false,false

MyTab:AddToggle({
	Name = "تأثير بايو",
	Callback = function(v)
		bioLoop = v
		if v then
			TextEffect("RolePlayBio", function() return myBio end, false, function() return bioLoop end)
		end
	end
})

MyTab:AddToggle({
	Name = "تأثير بايو عكسي",
	Callback = function(v)
		bioReverse = v
		if v then
			TextEffect("RolePlayBio", function() return myBio end, true, function() return bioReverse end)
		end
	end
})

MyTab:AddToggle({
	Name = "تأثير اسم",
	Callback = function(v)
		nameLoop = v
		if v then
			TextEffect("RolePlayName", function() return myName end, false, function() return nameLoop end)
		end
	end
})

MyTab:AddToggle({
	Name = "تأثير اسم عكسي",
	Callback = function(v)
		nameReverse = v
		if v then
			TextEffect("RolePlayName", function() return myName end, true, function() return nameReverse end)
		end
	end
})

--================ سبام الاسم =================--
MyTab:AddSection({ Name = "سبام الاسماء", Icon = "rbxassetid://10734933966" })

local spamActive = false
local spamDelay = 1
local list = {"","","","",""}

for i=1,5 do
	MyTab:AddTextBox({
		Name = "اسم "..i,
		Callback = function(v) list[i]=v end
	})
end

MyTab:AddSlider({
	Name="سرعة السبام",
	Min=1,Max=100,Default=5,
	Callback=function(v) spamDelay=v end
})

MyTab:AddToggle({
	Name="تشغيل السبام",
	Callback=function(v)
		spamActive=v
		if v then
			task.spawn(function()
				while spamActive do
					for _,n in ipairs(list) do
						if not spamActive then break end
						if n~="" then
							TextRemote:FireServer("RolePlayName",n)
							task.wait(1/spamDelay)
						end
					end
				end
			end)
		end
	end
})

--================ سبام البايو =================--
MyTab:AddSection({ Name = "سبام البايو", Icon = "rbxassetid://10734933966" })

local bioSpam=false
local bioList={"","","","",""}
local bioSpeed=1

for i=1,5 do
	MyTab:AddTextBox({
		Name="بايو "..i,
		Callback=function(v) bioList[i]=v end
	})
end

MyTab:AddSlider({
	Name="سرعة البايو",
	Min=1,Max=100,Default=5,
	Callback=function(v) bioSpeed=v end
})

MyTab:AddToggle({
	Name="تشغيل سبام البايو",
	Callback=function(v)
		bioSpam=v
		if v then
			task.spawn(function()
				while bioSpam do
					for _,b in ipairs(bioList) do
						if not bioSpam then break end
						if b~="" then
							TextRemote:FireServer("RolePlayBio",b)
							task.wait(1/bioSpeed)
						end
					end
				end
			end)
		end
	end
})

--================ تلوين الأدوات =================--
MyTab:AddSection({ Name = "تلوين الأدوات", Icon = "rbxassetid://10734950309" })

local RemoteFolder = RS:WaitForChild("RE")
local activeToolColor=false
local selectedTool="BabyRattle"

local ToolList={
["رول الطلاء"]="PaintRoller",
["روج"]="Lipstick",
["قوس"]="Bow",
["مسجل"]="Boombox",
["حافظة"]="Thermos",
["حلو"]="Donut",
["فرشة حصان"]="HorseBrush",
["عصي مضيئة"]="GlowingBatons",
["ميكرفون"]="Megaphone",
["شمعة"]="Candle",
["شعلة"]="GuardTorch",
["لعبة"]="BabyRattle"
}

MyTab:AddDropdown({
	Name="اختيار الأداة",
	Options={"رول الطلاء","روج","قوس","مسجل","حافظة","حلو","فرشة حصان","عصي مضيئة","ميكرفون","شمعة","شعلة","لعبة"},
	Default="لعبة",
	Callback=function(v)
		selectedTool=ToolList[v]
	end
})

local function holdTool(name)
	local char=LP.Character or LP.CharacterAdded:Wait()
	local hum=char:WaitForChild("Humanoid")
	local tool=LP.Backpack:WaitForChild(name,2)
	if tool then
		tool.Parent=char
		hum:EquipTool(tool)
	end
	return tool
end

local function rainbow(t)
	return Color3.fromHSV(t%1,1,1)
end

MyTab:AddToggle({
	Name="تشغيل تلوين الأداة",
	Callback=function(v)
		activeToolColor=v
		if not v then return end

		task.spawn(function()
			RemoteFolder:WaitForChild("1Clea1rTool1s"):FireServer("ClearAllTools")
			task.wait(0.2)

			RemoteFolder:WaitForChild("1Too1l"):InvokeServer("PickingTools",selectedTool)
			task.wait(0.3)

			local tool=holdTool(selectedTool)
			if not tool then return end

			local colorEvent=
				LP.PlayerGui:WaitForChild("ToolGui")
				:WaitForChild("ToolSettings")
				:WaitForChild("Settings")
				:WaitForChild("PropsColor")
				:WaitForChild("SetColor")

			local hue=0
			local current=rainbow(0)

			local conn
			conn=RSvc.RenderStepped:Connect(function(dt)
				if not activeToolColor then conn:Disconnect() return end
				hue+=dt*0.3
				local target=rainbow(hue)
				current=current:Lerp(target,dt*5)
				colorEvent:FireServer(current)
			end)
		end)
	end
})

--================ تلوين الشعر =================--
MyTab:AddSection({ Name="تلوين الشعر", Icon="rbxassetid://10734939942" })

local hairActive=false
local hairList={
	Color3.new(1,1,0),Color3.new(0,0,1),Color3.new(1,0,1),Color3.new(1,1,1),
	Color3.new(0,1,0),Color3.new(0.5,0,1),Color3.new(1,0.6,0),Color3.new(0,1,1)
}

MyTab:AddToggle({
	Name="تشغيل تلوين الشعر",
	Callback=function(v)
		hairActive=v
		if v then
			task.spawn(function()
				local i=1
				while hairActive do
					RS:WaitForChild("RE"):WaitForChild("1Max1y"):FireServer("ChangeHairColor2",hairList[i])
					task.wait(0.1)
					i=i%#hairList+1
				end
			end)
		end
	end
})

--================ تلوين الجسم =================--
MyTab:AddSection({ Name="تلوين الجسم", Icon="rbxassetid://10709782758" })

local BodyRemote=RS:WaitForChild("Remotes"):WaitForChild("ChangeBodyColor")

local bodyColors={
"Pastel brown","Pastel yellow","Pastel blue","Pastel green","Pastel pink",
"Really red","Bright orange","Bright blue","Bright violet","Bright green",
"Bright yellow","White","Black","Dark stone grey"
}

local bodyActive=false

MyTab:AddToggle({
	Name="تشغيل تلوين الجسم",
	Callback=function(v)
		bodyActive=v
		if v then
			task.spawn(function()
				while bodyActive do
					for _,c in ipairs(bodyColors) do
						if not bodyActive then break end
						BodyRemote:FireServer(c)
						task.wait(0.3)
					end
				end
			end)
		end
	end
})

--================ تلوين الدراجة =================--
MyTab:AddSection({ Name="تلوين الدراجة", Icon="rbxassetid://10734950309" })

local speedRGB=1

MyTab:AddSlider({
	Name="سرعة RGB",
	Min=1,Max=5,Default=3,
	Callback=function(v) speedRGB=v end
})

local function getRGB(spd)
	local h=(tick()*spd%5)/5
	return Color3.fromHSV(h,1,1)
end

local function send(eventName,args)
	local ev=RS:FindFirstChild("RE")
	if ev and ev:FindFirstChild(eventName) then
		pcall(function()
			ev[eventName]:FireServer(unpack(args))
		end)
	end
end

MyTab:AddToggle({
	Name="تلوين الدراجة / السكوتر",
	Callback=function(v)
		getgenv().bikeRGB=v
		task.spawn(function()
			while getgenv().bikeRGB do
				send("1Player1sCa1r",{"NoMotorColor",getRGB(speedRGB)})
				task.wait(0.1)
			end
		end)
	end
})

--================ فعالياتي =================--
local PlayTab = Window:MakeTab({ Title = "فعاليات", Icon = "rbxassetid://10723407498" })

local Plrs = game:GetService("Players")
local RS = game:GetService("ReplicatedStorage")
local LP = Plrs.LocalPlayer

--================ لوحات =================--
PlayTab:AddSection({ Name = "نظام اللوحات" })

local boardIndex = 1
local boardText = ""

local BoardPos = {
    [1] = CFrame.new(359.589081, 57.149670, 581.226135),
    [2] = CFrame.new(-634.203247, 25.374739, 361.505646),
    [3] = CFrame.new(-238.497910, 88.514732, -549.373962)
}

PlayTab:AddDropdown({
    Name = "اختيار اللوحة",
    Options = {"1","2","3"},
    Default = "1",
    Callback = function(v)
        boardIndex = tonumber(v)
    end
})

PlayTab:AddTextBox({
    Name = "النص",
    PlaceholderText = "By Hassouni",
    Callback = function(v)
        boardText = v
    end
})

PlayTab:AddButton({
    Name = "تنفيذ",
    Callback = function()
        if boardText == "" then return end

        local char = LP.Character
        if not char then return end

        local root = char:FindFirstChild("HumanoidRootPart")
        if not root then return end

        local old = root.CFrame
        root.CFrame = BoardPos[boardIndex]

        task.wait(0.8)

        RS.RE["1Cemeter1y"]:FireServer("ReturningCommercialWords", boardIndex, nil, boardText)

        task.wait(0.4)
        root.CFrame = old
    end
})

--================ هالة الكرة =================--
PlayTab:AddSection({ Name = "هالة الكرة" })

local BallSettings = {
    Speed = 2,
    Radius = 6,
    Height = 0,
    Push = false
}

PlayTab:AddSlider({
    Name = "السرعة",
    Min = 1,
    Max = 20,
    Default = 2,
    Callback = function(v) BallSettings.Speed = v end
})

PlayTab:AddSlider({
    Name = "الحجم",
    Min = 1,
    Max = 20,
    Default = 6,
    Callback = function(v) BallSettings.Radius = v end
})

PlayTab:AddSlider({
    Name = "الارتفاع",
    Min = -10,
    Max = 10,
    Default = 0,
    Callback = function(v) BallSettings.Height = v end
})

PlayTab:AddToggle({
    Name = "فلنق",
    Callback = function(v) BallSettings.Push = v end
})

local orbitOnTouch = false

local function startOrbit(char)
    if not char or not char:FindFirstChild("HumanoidRootPart") then return end

    local ballFolder = workspace.WorkspaceCom["001_SoccerBalls"]
    local ball = ballFolder:FindFirstChild("Soccer"..LP.Name)
    if not ball then return end

    ball.CanCollide = false
    ball.Massless = true

    task.spawn(function()
        while orbitOnTouch and ball and char do
            local t = tick() * BallSettings.Speed
            local offset = Vector3.new(
                math.cos(t)*BallSettings.Radius,
                BallSettings.Height,
                math.sin(t)*BallSettings.Radius
            )
            ball.Position = char.HumanoidRootPart.Position + offset
            task.wait()
        end
    end)
end

PlayTab:AddToggle({
    Name = "تفعيل الهالة",
    Callback = function(v)
        orbitOnTouch = v
        if v then
            startOrbit(LP.Character)
        end
    end
})

--================ ESP =================--
PlayTab:AddSection({ Name = "كشف اللاعبين" })

local espOn = false
local espFolder = Instance.new("Folder", workspace)
espFolder.Name = "MyESP"

local function addESP(plr)
    if not plr.Character or not plr.Character:FindFirstChild("Head") then return end

    local gui = Instance.new("BillboardGui", espFolder)
    gui.Adornee = plr.Character.Head
    gui.Size = UDim2.new(0,200,0,50)
    gui.AlwaysOnTop = true

    local txt = Instance.new("TextLabel", gui)
    txt.Size = UDim2.new(1,0,1,0)
    txt.BackgroundTransparency = 1
    txt.Text = plr.Name
    txt.TextScaled = true
    txt.TextColor3 = Color3.new(1,0,0)

    task.spawn(function()
        while espOn and gui.Parent do
            task.wait(0.1)
        end
        gui:Destroy()
    end)
end

PlayTab:AddToggle({
    Name = "تشغيل ESP",
    Callback = function(v)
        espOn = v
        if v then
            for _,p in ipairs(Plrs:GetPlayers()) do
                if p ~= LP then
                    addESP(p)
                end
            end
        else
            espFolder:ClearAllChildren()
        end
    end
})

--================ سبام قوارب =================--
PlayTab:AddSection({ Name = "سبام القوارب" })

PlayTab:AddToggle({
    Name = "تشغيل السبام",
    Callback = function(v)
        getgenv().boatSpam = v
        if v then
            task.spawn(function()
                while getgenv().boatSpam do
                    task.wait(0.5)
                end
            end)
        end
    end
})

--================ قنابل =================--
PlayTab:AddSection({ Name = "نظام القنابل" })

local bombCount = 5

PlayTab:AddDropdown({
    Name = "العدد",
    Options = {"5","10","20","30","40","50"},
    Default = "5",
    Callback = function(v)
        bombCount = tonumber(v)
    end
})

PlayTab:AddButton({
    Name = "تفجير",
    Callback = function()
        RS.RE["1Blo1wBomb1sServe1r"]:FireServer("Bomb"..LP.Name)
    end
})

PlayTab:AddButton({
    Name = "تنظيف",
    Callback = function()
        RS.RE["1Clea1rTool1s"]:FireServer("ClearAllTools")
    end
})

--================ ترسانتي =================--
local GearTab = Window:MakeTab({ Title = "تخريب قوي", Icon = "rbxassetid://10723416652" })

local RSvc = game:GetService("RunService")

--================ صندوق =================--
GearTab:AddSection({ Name = "نظام الصندوق" })

GearTab:AddButton({
    Name = "أخذ الصندوق",
    Callback = function()
        RS.RE["1Too1l"]:InvokeServer("PickingTools","PropMaker")
    end
})

--================ اختيار لاعب =================--
local targetPlayer = nil

local function listPlayers()
    local t = {}
    for _,p in ipairs(Plrs:GetPlayers()) do
        if p ~= LP then
            table.insert(t,p.Name)
        end
    end
    return t
end

local PlayerList = GearTab:AddDropdown({
    Name = "اختيار الهدف",
    Options = listPlayers(),
    Callback = function(v)
        targetPlayer = Plrs:FindFirstChild(v)
    end
})

GearTab:AddButton({
    Name = "تحديث اللاعبين",
    Callback = function()
        PlayerList:Set(listPlayers())
    end
})

--================ نظام البروبس =================--
local cache = {}
local lastUpdate = 0

local function myProps()
    if tick()-lastUpdate < 0.5 then return cache end
    cache = {}

    local folder = workspace:FindFirstChild("WorkspaceCom")
    if not folder then return cache end

    for _,f in ipairs(folder:GetChildren()) do
        for _,p in ipairs(f:GetChildren()) do
            if p.Name:find(LP.Name) and p:FindFirstChild("SetCurrentCFrame") then
                table.insert(cache,p)
            end
        end
    end

    lastUpdate = tick()
    return cache
end

local function moveProps(cf)
    for _,p in ipairs(myProps()) do
        pcall(function()
            p.SetCurrentCFrame:InvokeServer(cf)
        end)
    end
end

local function clearProps()
    RS.RE["1Clea1rTool1s"]:FireServer("ClearAllProps")
end

--================ تخريب =================--
local flingLoop, pullLoop, killLoop

GearTab:AddToggle({
    Name = "فلنق",
    Callback = function(v)
        if flingLoop then flingLoop:Disconnect() end

        if v and targetPlayer then
            flingLoop = RSvc.Heartbeat:Connect(function()
                local c = targetPlayer.Character
                local r = c and c:FindFirstChild("HumanoidRootPart")
                if r then
                    moveProps(r.CFrame)
                end
            end)
        end
    end
})

GearTab:AddToggle({
    Name = "سحب",
    Callback = function(v)
        if pullLoop then pullLoop:Disconnect() end

        if v and targetPlayer then
            pullLoop = RSvc.Heartbeat:Connect(function()
                local my = LP.Character and LP.Character:FindFirstChild("HumanoidRootPart")
                if my then
                    moveProps(my.CFrame)
                end
            end)
        end
    end
})

GearTab:AddToggle({
    Name = "إنهاء",
    Callback = function(v)
        if killLoop then killLoop:Disconnect() end

        if v and targetPlayer then
            killLoop = RSvc.Heartbeat:Connect(function()
                moveProps(CFrame.new(0,-9999,0))
            end)
        end
    end
})

--================ هالة شخصية =================--
GearTab:AddSection({ Name = "هالة شخصية" })

local auraOn = false
local radius = 6
local speed = 4

GearTab:AddSlider({
    Name = "الحجم",
    Min = 1,
    Max = 15,
    Default = 6,
    Callback = function(v) radius = v end
})

GearTab:AddSlider({
    Name = "السرعة",
    Min = 1,
    Max = 20,
    Default = 4,
    Callback = function(v) speed = v end
})

GearTab:AddToggle({
    Name = "تشغيل الهالة",
    Callback = function(v)
        auraOn = v
    end
})

RSvc.RenderStepped:Connect(function()
    if not auraOn then return end

    local char = LP.Character
    local hrp = char and char:FindFirstChild("HumanoidRootPart")
    if not hrp then return end

    local t = tick()*speed

    for i,p in ipairs(myProps()) do
        local angle = t + i
        local pos = hrp.CFrame * CFrame.new(
            math.cos(angle)*radius,
            0,
            math.sin(angle)*radius
        )

        pcall(function()
            p.SetCurrentCFrame:InvokeServer(pos)
        end)
    end
end)

--================ هالة على لاعب =================--
GearTab:AddSection({ Name = "هالة على هدف" })

local targetAuraPlayer = nil
local auraTargetOn = false
local auraRadius2 = 6
local auraSpeed2 = 6
local randomness = 0

local TargetDropdown = GearTab:AddDropdown({
    Name = "اختيار لاعب للهالة",
    Options = listPlayers(),
    Callback = function(v)
        targetAuraPlayer = Plrs:FindFirstChild(v)
    end
})

GearTab:AddButton({
    Name = "تحديث القائمة",
    Callback = function()
        TargetDropdown:Set(listPlayers())
    end
})

GearTab:AddSlider({
    Name = "حجم الهالة",
    Min = 1,
    Max = 20,
    Default = 6,
    Callback = function(v) auraRadius2 = v end
})

GearTab:AddSlider({
    Name = "عشوائية",
    Min = 0,
    Max = 5,
    Default = 0,
    Callback = function(v) randomness = v end
})

GearTab:AddSlider({
    Name = "سرعة",
    Min = 1,
    Max = 30,
    Default = 6,
    Callback = function(v) auraSpeed2 = v end
})

GearTab:AddToggle({
    Name = "تشغيل هالة على لاعب",
    Callback = function(v)
        auraTargetOn = v
    end
})

RSvc.RenderStepped:Connect(function()
    if not auraTargetOn then return end
    if not targetAuraPlayer or not targetAuraPlayer.Character then return end

    local hrp = targetAuraPlayer.Character:FindFirstChild("HumanoidRootPart")
    if not hrp then return end

    local t = tick() * auraSpeed2

    for i,p in ipairs(myProps()) do
        local angle = t + i
        local randX = (math.random()-0.5)*randomness*2
        local randZ = (math.random()-0.5)*randomness*2

        local pos = hrp.CFrame * CFrame.new(
            math.cos(angle)*auraRadius2 + randX,
            0,
            math.sin(angle)*auraRadius2 + randZ
        )

        pcall(function()
            p.SetCurrentCFrame:InvokeServer(pos)
        end)
    end
end)

--================ أدوات =================--
GearTab:AddSection({ Name = "أدوات خاصة" })

local function giveTool(name)
    RS.RE["1Playe1rTrigge1rEven1t"]:FireServer("AcceptedToolToServer", name, LP)
end

GearTab:AddButton({Name="Glider",Callback=function() giveTool("Glider") end})
GearTab:AddButton({Name="Cannon",Callback=function() giveTool("Cannon") end})
GearTab:AddButton({Name="Golden Ball",Callback=function() giveTool("GoldenBasketball") end})
GearTab:AddButton({Name="Water",Callback=function() RS.RE["1Too1l"]:InvokeServer("PickingTools","Water Balloon") end})
GearTab:AddButton({Name="Monkey",Callback=function() giveTool("BlueMonkey") end})
GearTab:AddButton({Name="Crystal",Callback=function() giveTool("Crystal") end})
GearTab:AddButton({Name="Uranium",Callback=function() giveTool("UraniumRod") end})
GearTab:AddButton({Name="Chainsaw",Callback=function() giveTool("Chainsaw") end})

--================ تنظيف =================--
GearTab:AddSection({ Name = "تنظيف" })

GearTab:AddButton({
    Name = "حذف الأدوات",
    Callback = function()
        RS.RE["1Clea1rTool1s"]:FireServer("ClearAllTools")
    end
})

GearTab:AddButton({
    Name = "حذف البروبس",
    Callback = function()
        clearProps()
    end
})

local Tab = Window:MakeTab({"لاق السيرفر", "bomb"})

-- قسم إيقاف السيرفر
local Section = Tab:AddSection({
    Name = "إيقاف السيرفر"
})

-- زر إيقاف السيرفر
Tab:AddButton({
    Name = "إيقاف السيرفر",
    Callback = function()
        for m = 1, 495 do
            local args = {
                [1] = "PickingTools",
                [2] = "FireHose"
            }
            game:GetService("ReplicatedStorage").RE:FindFirstChild("1Too1l"):InvokeServer(unpack(args))

            local args = {
                [1] = "FireHose",
                [2] = "DestroyFireHose"
            }
            game:GetService("Players").LocalPlayer.Backpack.FireHose.ToolSound:FireServer(unpack(args))
        end

        local oldcf = game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame
        game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(999999999.414, -475, 999999999.414)

        local rootpart = game.Players.LocalPlayer.Character.HumanoidRootPart
        repeat wait() until rootpart.Parent == nil
        repeat wait() until game.Players.LocalPlayer.Character:FindFirstChild("HumanoidRootPart")

        game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = oldcf

        game:GetService("StarterGui"):SetCore("SendNotification", {
            Title = "سكربت النسخ",
            Text = "تم إيقاف السيرفر، سيتم الإغلاق الآن",
            Button1 = "حسناً",
            Duration = 5
        })

        wait()

        for _, ergeg in pairs(game.Players.LocalPlayer.Backpack:GetChildren()) do
            if ergeg.Name == "FireHose" then
                ergeg.Parent = game.Players.LocalPlayer.Character
            end
        end

        wait(0.2)

        game:GetService("StarterGui"):SetCore("SendNotification", {
            Title = "سكربت النسخ",
            Text = "بدأ النسخ، انتظر قليلاً",
            Button1 = "حسناً",
            Duration = 5
        })

        wait(0.5)
        game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(9999, -475, 9999)
    end
})

-- زر إيقاف السيرفر (خطأ إنترنت)
Tab:AddButton({
    Name = "إيقاف السيرفر (خطأ في الإنترنت)",
    Callback = function()
        for m = 1, 535 do
            local args = {
                [1] = "PickingTools",
                [2] = "FireHose"
            }
            game:GetService("ReplicatedStorage").RE:FindFirstChild("1Too1l"):InvokeServer(unpack(args))

            local args = {
                [1] = "FireHose",
                [2] = "DestroyFireHose"
            }
            game:GetService("Players").LocalPlayer.Backpack.FireHose.ToolSound:FireServer(unpack(args))
        end

        local oldcf = game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame
        game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(999999999.414, -475, 999999999.414)

        local rootpart = game.Players.LocalPlayer.Character.HumanoidRootPart
        repeat wait() until rootpart.Parent == nil
        repeat wait() until game.Players.LocalPlayer.Character:FindFirstChild("HumanoidRootPart")

        game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = oldcf

        game:GetService("StarterGui"):SetCore("SendNotification", {
            Title = "سكربت النسخ",
            Text = "تم إيقاف السيرفر",
            Button1 = "حسناً",
            Duration = 5
        })

        wait()

        for _, ergeg in pairs(game.Players.LocalPlayer.Backpack:GetChildren()) do
            if ergeg.Name == "FireHose" then
                ergeg.Parent = game.Players.LocalPlayer.Character
            end
        end

        wait(0.2)

        game:GetService("StarterGui"):SetCore("SendNotification", {
            Title = "سكربت النسخ",
            Text = "بدأ النسخ، انتظر",
            Button1 = "حسناً",
            Duration = 5
        })

        wait(0.5)
        game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(9999, -475, 9999)
    end
})

-- قسم اللاغ باستخدام اللابتوب
local Section = Tab:AddSection({
    Name = "لاق باستخدام اللابتوب"
})

local toggles = { LagLaptop = false }

local function clickLaptopPart(part)
    local clickDetector = part:FindFirstChildWhichIsA("ClickDetector")
    if clickDetector then
        fireclickdetector(clickDetector)
    end
end

local function lagarJogoLaptop(laptopPart, maxClicks)
    local character = game.Players.LocalPlayer.Character or game.Players.LocalPlayer.CharacterAdded:Wait()
    local hrp = character:WaitForChild("HumanoidRootPart")

    local clickCount = 0
    while toggles.LagLaptop and clickCount < maxClicks do
        hrp.CFrame = laptopPart.CFrame + Vector3.new(0, 3, 0)
        clickLaptopPart(laptopPart)
        clickCount += 1
        task.wait(0.05)
    end
end

Tab:AddToggle({
    Name = "تفعيل لاق اللابتوب",
    Default = false,
    Callback = function(state)
        toggles.LagLaptop = state
        if state then
            local laptopPart = workspace:FindFirstChild("WorkspaceCom")
                and workspace.WorkspaceCom:FindFirstChild("001_GiveTools")
                and workspace.WorkspaceCom["001_GiveTools"]:FindFirstChild("Laptop")

            if laptopPart and laptopPart:IsA("BasePart") then
                spawn(function()
                    lagarJogoLaptop(laptopPart, 999999999)
                end)
            end
        end
    end
})

Tab:AddParagraph({
"معلومات اللاغ",
"تأثير اللاغ يبدأ بعد 35 ثانية"
})

-- قسم اللاغ باستخدام الهاتف
local Section = Tab:AddSection({
    Name = "لاق باستخدام الهاتف"
})

toggles.LagPhone = false

local function clickPhonePart(part)
    local clickDetector = part:FindFirstChildWhichIsA("ClickDetector")
    if clickDetector then
        fireclickdetector(clickDetector)
    end
end

local function lagarJogoPhone(phonePart, maxClicks)
    local player = game.Players.LocalPlayer
    local character = player.Character or player.CharacterAdded:Wait()
    local hrp = character:WaitForChild("HumanoidRootPart")

    local clickCount = 0
    while toggles.LagPhone and clickCount < maxClicks do
        hrp.CFrame = phonePart.CFrame + Vector3.new(0, 3, 0)
        clickPhonePart(phonePart)
        clickCount += 1
        task.wait(0.05)
    end
end

Tab:AddToggle({
    Name = "تفعيل لاق الهاتف",
    Default = false,
    Callback = function(state)
        toggles.LagPhone = state
    end
})

Tab:AddParagraph({
"معلومات اللاغ",
"تأثير اللاغ يبدأ بعد 35 ثانية"
})

-- قسم اللاغ باستخدام القنبلة
local Section = Tab:AddSection({
    Name = "لاق باستخدام القنبلة"
})

local BombActive = false

Tab:AddToggle({
    Name = "تفعيل لاق القنبلة",
    Default = false,
    Callback = function(Value)
        BombActive = Value
    end
})

Tab:AddParagraph({
"معلومات اللاغ",
"تأثير اللاغ يبدأ بعد 35 ثانية"
})

local Tab4 = Window:MakeTab({ Title = "نسخ سكنات", Icon = "rbxassetid://10734952036" })
Tab4:AddSection({ Name = "          نسخ سكنات اول شي", Icon = "rbxassetid://10734952036" })
local Players = game:GetService("Players")
local Rep = game:GetService("ReplicatedStorage")
local LocalPlayer = Players.LocalPlayer
local lastValidTarget = nil
local copyType = "بروكهافن"
local Target = nil

local function Wear(id)
    pcall(function()
        Rep.Remotes.Wear:InvokeServer(tonumber(id))
    end)
end

local function RESETBLOCK()
     args = {
        [1] = {0, 0, 0, 0, 0, 0},
        [2] = "AllBlocky"
    }
    pcall(function()
        Rep.Remotes.ChangeCharacterBody:InvokeServer(unpack(args))
    end)
end

local function ApplySkinToneFromUserId(userId)
    pcall(function()
        local info = Players:GetCharacterAppearanceInfoAsync(userId)
        if info.bodyColors then
            local headColor = info.bodyColors.HeadColor
            if headColor then
                Rep.Remotes.ChangeBodyColor:FireServer(tostring(headColor))
            end
        end
    end)
end

local function CopyClothing(desc)
    local items = {desc.Shirt, desc.Pants, desc.GraphicTShirt, desc.Face}
    for _, id in ipairs(items) do
        if tonumber(id) and id ~= 0 then
            Wear(id)
            task.wait(0.1)
        end
    end
end

local function CopyAccessories(desc)
    pcall(function()
        for _, v in ipairs(desc:GetAccessories(true)) do
            if v.AssetId then
                Wear(v.AssetId)
                task.wait(0.1)
            end
        end
    end)
end

local function CopyBodyParts(desc, source)
     args = {
        [1] = {
            tonumber(desc.Torso),
            tonumber(desc.RightArm),
            tonumber(desc.LeftArm),
            tonumber(desc.RightLeg),
            tonumber(desc.LeftLeg),
            tonumber(desc.Head)
        },
        [2] = source
    }
    pcall(function()
        Rep.Remotes.ChangeCharacterBody:InvokeServer(unpack(args))
    end)
end

local function CopyAnimations(desc)
    local anims = {
        desc.IdleAnimation,
        desc.WalkAnimation,
        desc.RunAnimation,
        desc.JumpAnimation,
        desc.FallAnimation,
        desc.SwimAnimation
    }
    for _, animId in ipairs(anims) do
        if tonumber(animId) and animId ~= 0 then
            Wear(animId)
            task.wait(0.1)
        end
    end
end

local function CopyBrookhaven(targetPlayer)
    local humanoid = targetPlayer.Character and targetPlayer.Character:FindFirstChildOfClass("Humanoid")
    if not humanoid then return end

    local desc = humanoid:GetAppliedDescription()
    local bodyColors = targetPlayer.Character:FindFirstChildOfClass("BodyColors")
    if bodyColors then
        local headColor = bodyColors.HeadColor
        if headColor then
            Rep.Remotes.ChangeBodyColor:FireServer(tostring(headColor))
        end
    end

    pcall(function()
        CopyAccessories(LocalPlayer.Character:FindFirstChildOfClass("Humanoid"):GetAppliedDescription())
        RESETBLOCK()
        CopyAccessories(desc)
        task.wait(0.1)
        CopyBodyParts(desc, "LOC4THub")
        CopyClothing(desc)
        CopyAnimations(desc)
    end)
end

local function CopyOriginalAvatar(userId)
    pcall(function()
        local desc = Players:GetHumanoidDescriptionFromUserId(userId)
        CopyAccessories(LocalPlayer.Character:FindFirstChildOfClass("Humanoid"):GetAppliedDescription())
        RESETBLOCK()
        CopyAccessories(desc)
        task.wait(0.1)
        CopyBodyParts(desc, "LOC4THub")
        CopyClothing(desc)
        CopyAnimations(desc)
        ApplySkinToneFromUserId(userId)
    end)
end

local function findPlayerByName(partialName)
    if not partialName or partialName == "" then return nil end
    partialName = partialName:lower()
    
    local foundPlayers = {}
    for _, player in ipairs(Players:GetPlayers()) do
        if player ~= LocalPlayer and player.Name:lower():find(partialName, 1, true) then
            table.insert(foundPlayers, player)
        end
    end

    table.sort(foundPlayers, function(a, b)
        local aStart = a.Name:lower():sub(1, #partialName) == partialName
        local bStart = b.Name:lower():sub(1, #partialName) == partialName
        if aStart and not bStart then return true end
        if not aStart and bStart then return false end
        return #a.Name < #b.Name
    end)
    
    return foundPlayers[1]
end

Tab4:AddTextBox({
    Name = "اسم اللاعب",
    Default = "",
    Callback = function(text)
        if text == "" then
            if lastValidTarget then
                text = lastValidTarget
            else
                return
            end
        end
        local targetPlayer = findPlayerByName(text)
        if targetPlayer then
            Target = targetPlayer
            lastValidTarget = targetPlayer.Name
        end
    end
})

Tab4:AddDropdown({
    Name = "طريقة النسخ",
    Options = {"بروكهافن", "سكن لاساسي"},
    Callback = function(option)
        copyType = option
    end
})

Tab4:AddButton({
    Name = "نسخ",
    Callback = function()
        if not Target then return end
        if copyType == "بروكهافن" then
            CopyBrookhaven(Target)
        else
            CopyOriginalAvatar(Target.UserId)
        end
    end
})
Tab4:AddSection({ Name = "          الثاني نسخ سكنات", Icon = "rbxassetid://10734952036" })

local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Remotes = ReplicatedStorage:WaitForChild("Remotes")

local Target = nil

local function GetPlayerNames()
    local PlayerNames = {}
    for _, player in ipairs(Players:GetPlayers()) do
        table.insert(PlayerNames, player.Name)
    end
    return PlayerNames
end

local Dropdown = Tab4:AddDropdown({
    Name = "اختيار لاعب",
    Options = GetPlayerNames(),
    Default = Target,
    Callback = function(Value)
        Target = Value
    end
})

local function UpdateDropdown()
    Dropdown:Refresh(GetPlayerNames(), true)
end

Players.PlayerAdded:Connect(UpdateDropdown)
Players.PlayerRemoving:Connect(UpdateDropdown)
Tab4:AddButton({
    Name = "نسخ",
    Callback = function()
        if not Target then return end

        local LP = Players.LocalPlayer
        local LChar = LP.Character
        local TPlayer = Players:FindFirstChild(Target)

        if TPlayer and TPlayer.Character then
            local LHumanoid = LChar and LChar:FindFirstChildOfClass("Humanoid")
            local THumanoid = TPlayer.Character:FindFirstChildOfClass("Humanoid")

            if LHumanoid and THumanoid then
                local LDesc = LHumanoid:GetAppliedDescription()

                for _, acc in ipairs(LDesc:GetAccessories(true)) do
                    if acc.AssetId and tonumber(acc.AssetId) then
                        Remotes.Wear:InvokeServer(tonumber(acc.AssetId))
                        task.wait(0.2)
                    end
                end

                if tonumber(LDesc.Shirt) then
                    Remotes.Wear:InvokeServer(tonumber(LDesc.Shirt))
                    task.wait(0.2)
                end

                if tonumber(LDesc.Pants) then
                    Remotes.Wear:InvokeServer(tonumber(LDesc.Pants))
                    task.wait(0.2)
                end

                if tonumber(LDesc.Face) then
                    Remotes.Wear:InvokeServer(tonumber(LDesc.Face))
                    task.wait(0.2)
                end

                local PDesc = THumanoid:GetAppliedDescription()

                local argsBody = {
                    [1] = {
                        [1] = PDesc.Torso,
                        [2] = PDesc.RightArm,
                        [3] = PDesc.LeftArm,
                        [4] = PDesc.RightLeg,
                        [5] = PDesc.LeftLeg,
                        [6] = PDesc.Head
                    }
                }
                Remotes.ChangeCharacterBody:InvokeServer(unpack(argsBody))
                task.wait(0.5)

                if tonumber(PDesc.Shirt) then
                    Remotes.Wear:InvokeServer(tonumber(PDesc.Shirt))
                    task.wait(0.3)
                end

                if tonumber(PDesc.Pants) then
                    Remotes.Wear:InvokeServer(tonumber(PDesc.Pants))
                    task.wait(0.3)
                end

                if tonumber(PDesc.Face) then
                    Remotes.Wear:InvokeServer(tonumber(PDesc.Face))
                    task.wait(0.3)
                end

                for _, v in ipairs(PDesc:GetAccessories(true)) do
                    if v.AssetId and tonumber(v.AssetId) then
                        Remotes.Wear:InvokeServer(tonumber(v.AssetId))
                        task.wait(0.3)
                    end
                end

                local SkinColor = TPlayer.Character:FindFirstChild("Body Colors")
                if SkinColor then
                    Remotes.ChangeBodyColor:FireServer(tostring(SkinColor.HeadColor))
                    task.wait(0.3)
                end

                if tonumber(PDesc.IdleAnimation) then
                    Remotes.Wear:InvokeServer(tonumber(PDesc.IdleAnimation))
                    task.wait(0.3)
                end

                local Bag = TPlayer:FindFirstChild("PlayersBag")
                if Bag then
                    if Bag:FindFirstChild("RPName") and Bag.RPName.Value ~= "" then
                        Remotes.RPNameText:FireServer("RolePlayName", Bag.RPName.Value)
                        task.wait(0.3)
                    end
                    if Bag:FindFirstChild("RPBio") and Bag.RPBio.Value ~= "" then
                        Remotes.RPNameText:FireServer("RolePlayBio", Bag.RPBio.Value)
                        task.wait(0.3)
                    end
                    if Bag:FindFirstChild("RPNameColor") then
                        Remotes.RPNameColor:FireServer("PickingRPNameColor", Bag.RPNameColor.Value)
                        task.wait(0.3)
                    end
                    if Bag:FindFirstChild("RPBioColor") then
                        Remotes.RPNameColor:FireServer("PickingRPBioColor", Bag.RPBioColor.Value)
                        task.wait(0.3)
                    end
                end
            end
        end
    end
})

loadstring(game:HttpGet("https://raw.githubusercontent.com/hassounijamel-cpu/0777/refs/heads/main/%D8%AA%D8%A7%D8%A8%20%D8%A7%D8%BA%D8%A7%D9%86%D9%8A.txt"))()
